[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "gunicorn src.app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 600 --keep-alive 5 --max-requests 1000 --max-requests-jitter 50"
